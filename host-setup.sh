#!/usr/bin/env bash

# Get OS and architecture to do OS specific actions.
OS="$(uname)"
ARC="$(uname -m)"

# @TODO: Take the environment as argument, maybe something like gui or no-gui and run the other scripts based on that.
